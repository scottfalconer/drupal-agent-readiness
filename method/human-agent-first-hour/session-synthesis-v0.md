# First-hour session synthesis v0

Session ID: __________  Primary coder: __________  Second coder: __________

## Registered session context

- Instrument version/hash:
- Install guidance: `full_recipe_v0` | `constraints_only_v0`
- Install-card version/hash:
- Install discovery pre-solved: `true` | `false`
- Agent stack:
- Protocol deviations:

## Outcome summary

- T1–T6:
- `verified_governed_value_at_60`:
- `verified_governed_value_at_registered_stop`:
- Timing status (registered duration / elapsed / poll offset / standard hour):
- Readout status: `complete` | `partial` | `incomplete` (`partial` preserves
  valid technical outcomes and nulls only affected lanes)
- M1 / M4 / MG (secondary):
- `steering_exposed`: `true` | `false`
- `steering_trigger_source`: `m4` | `participant_statement` | `not_reached`
- Steering trigger clock:
- Steering result: `pass` | `partial` | `fail` | `not_reached`
- Transfer precondition / outcome / assistance:
- Belief categories and linked `CLAIM`/`VERIFY` evidence:

## Interaction coding

Coding status: `complete` | `unavailable`  Unavailable reason: __________

Report `DL-HARD` and `DL-SOFT` separately as raw counts and per 10 participant
turns. Every leakage event requires a controlled `decision_object`. Every
`DL-HARD`, `DL-SOFT`, `DE`, `R-HUMAN`, and false-confident belief requires a
`failure_scope` and `candidate_fix`. Do not turn `agent_generic` findings into
Drupal backlog items.

Also report hard/soft leakage frequency by `decision_object`; this frequency
table is the direct input to candidate backlog prioritization.

## Top three observed frictions

Rank by supported minutes until progress resumed, then severity when duration
is not estimable. Use `not_estimable`, never zero, when evidence cannot support
a duration.

| Rank | Event IDs | Friction | Observable minutes until progress resumed | Affected milestone/outcome | Scope | Candidate fix | Evidence |
| --- | --- | --- | ---: | --- | --- | --- | --- |
| 1 |  |  |  |  |  |  |  |
| 2 |  |  |  |  |  |  |  |
| 3 |  |  |  |  |  |  |  |

## Participant debrief

What one change would have most improved this hour?

Response: _________________________________________________________________

This response is hypothesis-generating, not causal evidence.
